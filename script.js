(function () {
  'use strict'
    
  // string to store the values from user input
  const valueStore = ''
  
  // add eventListener to buttons class normalButton, when pressed add they're value to value store, and display it in displaySection
  
  // add eventListener to equalsButton when pressed perform evaluation and display result
}())
